project('systemTime',
        'c',
        version : '0.0.1',
        default_options : ['warning_level=3', 'default_library=static'])

library('systemTime', 'src/systemTime.c', install: true)

install_headers('include/systemTime.h', 
                subdir: 'systemTime')